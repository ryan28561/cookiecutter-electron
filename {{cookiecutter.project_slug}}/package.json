{
  "name": "{{ cookiecutter.project_slug }}",
  "version": "0.0.1",
  "description": "{{ cookiecutter.description }}",
  "main": "index.js",
  "scripts": {
    "start": "webpack-dev-server --hotOnly",
    "build": "webpack -p"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/{{cookiecutter.author}}/{{cookiecutter.project_slug}}.git"
  },
  "author": "{{ cookiecutter.author }}",
  "dependencies": {
  },
  "devDependencies": {
  }
}
